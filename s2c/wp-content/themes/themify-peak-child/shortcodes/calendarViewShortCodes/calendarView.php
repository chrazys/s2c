<?php
// function refresh_other_shortcode()
// {
//     // Get the selected values from the AJAX request
//     $selected_values = sanitize_text_field($_POST['selected_values']);

//     // Use the selected values to generate the content for the other shortcode
//     $other_shortcode_content = '[other_shortcode selected_values="' . $selected_values . '"]';

//     echo do_shortcode($other_shortcode_content);

//     wp_die();
// }


